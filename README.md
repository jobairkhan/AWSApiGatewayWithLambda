# AWSApiGatewayWithLambda
A vanilla lambda that should triggered by AWS Api Gateway

# Deploy
To deply the package

```
dotnet lambda package -c Release -o ../LambdaTriggerByApi.zip -f netcoreapp2.1
```
